<%= render :partial => 'admin/shared/configuration_menu' %>

<h1><%= t("editing_recommendation_provider") %></h1>
<%= error_messages_for :recommendation_provider %>
<% form_for(@recommendation_provider, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag t("update")%>
  </p>
<% end %>