<%= render :partial => 'admin/shared/configuration_menu' %>

<h1><%= t("new_recommendation_provider") %></h1>
<%= error_messages_for :recommendation_provider %>

<% form_for(:recommendation_provider, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag t("create") %>
  </p>
<% end %>